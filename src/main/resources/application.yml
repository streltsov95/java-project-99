---

logging:
  level:
    root: WARN

spring:
  application:
    name: app
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  output:
    ansi:
      enabled: always
  profiles:
    active: development

server:
  address: 0.0.0.0
  port: ${PORT:8080}
